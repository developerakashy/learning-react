const Welcome = (props) => {
    return (
        <div>
            <h1>Welcome, { props.name }! a.k.a { props.name } { props.surName }</h1>
            
        </div>
    )
}

export default Welcome
